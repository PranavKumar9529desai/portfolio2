import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "fast.wistia.com", "res.cloudinary.com", "player.cloudinary.com"],
  },
};

export default nextConfig;
